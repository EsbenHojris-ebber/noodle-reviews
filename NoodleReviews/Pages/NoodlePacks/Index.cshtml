@page
@model NoodleReviews.Pages.NoodlePacks.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Nuddelanmeldelser</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>

<form>
    <p>
        <select asp-for="NoodlePackGrade" asp-items="Model.Grades">
            <Option value="">Alle</Option>
        </select>
        Navn: <input type="text" asp-for="SearchString" \>
        <input type="submit" value="Filtrér" \>
    </p>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NoodlePack[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoodlePack[0].BarCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoodlePack[0].Notes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoodlePack[0].ReviewDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoodlePack[0].Grade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoodlePack[0].Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.NoodlePack) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BarCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Notes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReviewDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Grade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
